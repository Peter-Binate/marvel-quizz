import React from 'react';

// On créé un contexte avec la valeur initial null que l'on va insérer dans un objet FirebaseContext
const FirebaseContext = React.createContext(null);

export default FirebaseContext;